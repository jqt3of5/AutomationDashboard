import React, {useState, useEffect} from 'react';import './common.css';import './TestList.css'import firebase from './Firebase/Firestore.js'import Test from "./Firebase/Test";import {RoutedTable} from "./Shared/Table"export default function TestList (props) {    const [tests, setTests] = useState([])    useEffect(() => {        let db = firebase.firestore()        db            .collection("Tests")            .get().then(            query => {                query.forEach(doc => {                    Test.getTest(doc).then(test => {                        setTests(state => state.concat(test))                    })                })            }        )    },[])    return (<div>        <div className={"content-header"}>            <div className={"content-header-inner"}>            Tests            </div>        </div>        <RoutedTable columns={{"TestFixture":"fixture.name", "Test":"name"}} idField = {"testId"} data={tests}></RoutedTable>    </div>)}